apiVersion: v1
kind: Service
metadata:
  name: auth-postgres-service
  namespace: microservices
  labels:
    app: auth-postgres
    component: database
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: auth-postgres

