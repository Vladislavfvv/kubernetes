apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: microservices
  labels:
    app: user-service
    component: microservice
spec:
  type: ClusterIP  # Для внутреннего доступа между сервисами, внешний доступ через Ingress
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: user-service

